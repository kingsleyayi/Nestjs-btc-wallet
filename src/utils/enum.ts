export enum genderEnum {
  MALE = 'male',
  FEMALE = 'female',
}

export enum userStatus {
  ACTIVE = 'active',
  SUSPEND = 'suspend',
}

export enum TransactionType {
  IN = 'in',
  OUT = 'out',
}

export enum INVESMENTENUM {
  basic = 500,
  silver = 1000,
  gold = 2000,
  premium = 5000,
}
